#!/usr/bin/python3
"""Define this state class."""
from model.base_models import BaseModel

class State(BaseModel):
    """Represent a state.
    Attributes:
        name (str): Name of the state."""
    name = ""        
